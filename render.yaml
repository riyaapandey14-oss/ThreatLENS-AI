services:
  - type: web
    name: threatlens-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python .vscode/run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: FLASK_ENV  
        value: production
    autoDeploy: true
